import 'package:flutter/material.dart';

class AppColors {
  // bottomNavColors
  static const bottomNavBarItemSelectedColor = Colors.deepOrangeAccent;
  static const bottomNavBarItemUnSelectedColor = Colors.white60;
  static final bottomNavBgColor = Colors.black.withOpacity(0.5);

  //

  static const scaffoldBackgroundColor = Color(0xFF181616);

  //

  static final textCategoryHeadingColor = Colors.white.withOpacity(0.7);

  static final textWhiteColor = Colors.white.withOpacity(0.7);

  static const errorColor = Colors.red;
}
